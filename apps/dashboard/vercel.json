{
  "buildCommand": "cd ../.. && npm run build:dashboard",
  "outputDirectory": "apps/dashboard/dist",
  "devCommand": "npm run dev",
  "installCommand": "cd ../.. && npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/dashboard/:path*",
      "destination": "/dashboard/:path*"
    }
  ]
}
