{
  "buildCommand": "cd ../.. && vite build --config apps/auth/vite.config.ts",
  "outputDirectory": "apps/auth/dist",
  "devCommand": "npm run dev",
  "installCommand": "cd ../.. && npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/auth/:path*",
      "destination": "/auth/:path*"
    }
  ]
}
