{
  "buildCommand": "cd ../.. && npm run build:admin",
  "outputDirectory": "apps/admin/dist",
  "devCommand": "npm run dev",
  "installCommand": "cd ../.. && npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/admin/:path*",
      "destination": "/admin/:path*"
    }
  ]
}
