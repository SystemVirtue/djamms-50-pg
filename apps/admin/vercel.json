{
  "buildCommand": "cd ../.. && vite build --config apps/admin/vite.config.ts",
  "outputDirectory": "apps/admin/dist",
  "devCommand": "npm run dev",
  "installCommand": "cd ../.. && npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/admin/:path*",
      "destination": "/admin/:path*"
    }
  ]
}
