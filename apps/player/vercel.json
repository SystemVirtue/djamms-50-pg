{
  "buildCommand": "cd ../.. && npm run build:player",
  "outputDirectory": "apps/player/dist",
  "devCommand": "npm run dev",
  "installCommand": "cd ../.. && npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/player/:path*",
      "destination": "/player/:path*"
    }
  ]
}
