{
  "buildCommand": "cd ../.. && npm run build:kiosk",
  "outputDirectory": "apps/kiosk/dist",
  "devCommand": "npm run dev",
  "installCommand": "cd ../.. && npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/kiosk/:path*",
      "destination": "/kiosk/:path*"
    }
  ]
}
